{
    "builds": [{
        "src": "youtube/wsgi.py",
        "use": "@NatanFS/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "youtube/wsgi.py"
        }
    ]
}
