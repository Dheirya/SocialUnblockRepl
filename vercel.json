{
    "builds": [{
        "src": "youtube/wsgi.py",
        "use": "@Dheirya/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "youtube/wsgi.py"
        }
    ]
}
